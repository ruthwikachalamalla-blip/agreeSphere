import { Link } from 'react-router-dom'; import { ArrowUpRight, Star } from 'lucide-react';
export const fallbackImage = 'https://images.unsplash.com/photo-1499529112087-3cb3b73cebd8?auto=format&fit=crop&w=900&q=80';
export function ProductCard({ product, onAdd }) { const img = product.image || fallbackImage; return <article className="product-card"><Link to={`/products/${product._id}`} className="product-image"><img src={img} alt={product.name} onError={e => { e.currentTarget.src = fallbackImage; }}/><span className="product-tag">{product.category}</span><span className="image-arrow"><ArrowUpRight size={19}/></span></Link><div className="product-info"><div className="product-title-line"><Link to={`/products/${product._id}`} className="product-title">{product.name}</Link><span className="rating"><Star size={12} fill="currentColor"/> {Number(product.rating || 4.5).toFixed(1)}</span></div><p className="product-meta">{product.seller?.name || product.unit || 'Farm essential'}</p><div className="product-buy"><strong>₹{Number(product.price).toLocaleString('en-IN')} <span>/ {product.unit || 'each'}</span></strong><button className="add-circle" aria-label={`Add ${product.name} to bag`} onClick={() => onAdd?.(product)}>+</button></div></div></article>; }
